const styles = theme => ({
    root: {
        paddingLeft: theme.spacing(3),
        paddingRight: theme.spacing(3),
    },
    container: {
        minHeight: "100vh",
    },
});

export default styles;